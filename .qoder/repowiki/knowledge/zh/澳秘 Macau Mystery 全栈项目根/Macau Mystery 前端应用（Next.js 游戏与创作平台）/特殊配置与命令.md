开发启动 `npm run dev`（即 `next dev`），构建 `npm run build`，生产运行 `npm start`；需配置 `.env.local` 中的 `NEXT_PUBLIC_API_URL`（默认 `http://localhost:8000/api/v1`），并通过 `next.config.ts` 的 rewrite 规则将 `/api/v1/*` 转发至后端服务。