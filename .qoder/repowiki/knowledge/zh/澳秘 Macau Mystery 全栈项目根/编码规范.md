- 后端 JSON 字段统一 snake_case，前端仅在 API 层做适配，禁止散落到页面组件中
- 新持久化逻辑必须复用既有数据库模型与事务，不得新增全局内存数据源
- 剧情 JSON 以 chapters → scenes → choices 为核心结构，choice 跳转用 next_scene，线索通过 grant_clues 数组发放
- 用户可见文案通过 useTranslation() 获取，新增翻译键须同时补齐三种 locale
- 提交信息沿用 feat:/fix:/docs:/refactor:/test: 前缀规范
- 容器内 DATABASE_URL 指向持久化卷，生产部署关闭 BOOTSTRAP_DEMO_STORY 与演示账号