根级 `docker compose up --build` 一键启动后端（含 Alembic 迁移与 SQLite 持久卷），前端需单独 `cd frontend && npm ci && npm run dev`；后端本地开发使用 `uvicorn app.main:app --reload --port 8000`，Swagger 文档在 `/docs`，健康检查 `/api/v1/health`。