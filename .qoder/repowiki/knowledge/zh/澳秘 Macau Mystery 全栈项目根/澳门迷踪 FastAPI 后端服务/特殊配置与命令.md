运行：`uvicorn app.main:app --host 0.0.0.0 --port 8000`（Dockerfile 已内置）。首次启动需先执行 `alembic upgrade head` 初始化数据库表，否则 health 端点返回 503。可通过 `BOOTSTRAP_DEMO_STORY=1` 和 `BOOTSTRAP_DEMO_USERS=1` 自动注入演示剧情与 admin/guest 账号。测试使用 pytest：`pytest tests/`，配置文件见 `pytest.ini`。